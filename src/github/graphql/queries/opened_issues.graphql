query($after: String) {
  viewer {
    issues(
      first: 100
      after: $after
      orderBy: { field: CREATED_AT, direction: DESC }
    ) {
      pageInfo {
        hasNextPage
        endCursor
      }
      nodes {
        createdAt
        url
        title
        repository {
          nameWithOwner
        }
      }
    }
  }
}
